import React, { FC } from 'react'
import PipeSelect from '../components/PipeSelect';

const Marking: FC = () => {
    return(
        <div className=''>
            <header>
                <PipeSelect/>
            </header>
        </div>
    );
};

export default Marking;